---
node_id: T4.5
node_name: 契约校验
node_type: auto
timeout_hours: 4
responsibility: 前后端联调负责人
check_script: ./scripts/contract-validation.sh
next_node: T4.6
rollback_to: T4
---

# T4.5 - 契约校验

前后端开发完成后的第一个会合点，确保 API 契约一致。

## ✅ 完成标准（DoD）
- 契约一致性检查通过
- 前端类型重新生成无误
- 后端路由覆盖率 ≥ 90%
- 核心接口联调通过

## 🔍 检查项

| 检查项 | 级别 | 通过标准 |
|--------|------|---------|
| OpenAPI Spec 存在 | P0 | 文件存在且格式合法 |
| 前端类型已生成 | P1 | api.generated.ts 最新 |
| 后端路由覆盖率 | P1 | ≥ 90% 与 Spec 一致 |
| 错误码一致性 | P2 | 前后端统一 |

## 🤖 自动执行逻辑

```bash
# 步骤 1：运行契约检查
./scripts/contract-validation.sh

# 步骤 2：变更影响分析
./scripts/contract-change-impact.sh

# 步骤 3：自动归档报告
./scripts/archive-node-docs.sh T4.5 <功能名称>
```

## 🔙 回滚触发条件
- P0 级不匹配
- 路由覆盖率 < 50%
- 类型生成失败

回退到 **T4**，前后端各自修复。

## 📦 归档产出物
- `docs/api/Contract-Validation-Report-YYYYMMDD.md` - 契约验证报告
- `docs/api/Change-Impact-Analysis-YYYYMMDD.md` - 变更影响分析

## ⚠️ 常见冲突场景
1. **后端改了 Spec 没通知前端**
2. **前端用了旧版本的类型文件**
3. **分页参数命名不一致（page vs page_num）**
